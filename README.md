# SysCall-SYS_SET_SCHED_HINT
By modifying the core data structures and scheduling algorithms of the Linux kernel (v5.19.8), this study introduces a custom system call architecture that empowers applications to explicitly declare their operational profiles to the kernel.
